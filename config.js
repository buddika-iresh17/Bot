module.exports = {
  PREFIX: ".",               // Bot commands prefix
  MODE: "public",            // Bot mode (public/private)
  OWNER_NUM: "94721551183",  // Owner phone number with country code (without +)
  SESSION_ID: "szVhzB5Q#YZM90WNczC-TLS2tYtpmkQU5Htc-bBbMM4JU0-empyk",  // Your mega.nz session id for creds.json download
  AUTO_STATUS_SEEN: true,    // true to enable auto view statuses feature

  // Optional flags if you want to use here to toggle features globally
  ANTI_LINK_ENABLED: true,   // true to enable antilink feature globally by default (can override per group)
  ANTI_DELETE_ENABLED: true  // true to enable antidelete feature globally by default (can override per group)
};